# 📐 [프로젝트 지침서] KB선도 50 대장주 아파트 실거래가 및 급매 대조 웹서비스

## 1. 프로젝트 개요 & 핵심 철학 (Core Philosophy)

* **프로젝트명:** KB Top 50 아파트 실거래 MDD & 급매 대조 시스템 
* **기획 의도:** 호가 위주의 부동산 지수(부동산원 등)와 자극적인 언론 보도로 인한 시장 왜곡 현상을 바로잡고, **국토교통부 실제 체결 실거래가 데이터** 및 **실제 최저가 급매 호가**를 대조하여 일반 시민들에게 왜곡 없는 정확한 시장 하락률(MDD) 정보를 제공한다.
* **주요 대상:** 전국 시가총액 상위 50개 대장주 단지 (`KB선도아파트 50` 기준)
* **핵심 가치:** 신뢰성, 투명성, 직관적인 데이터 시각화 및 프리미엄 UI/UX

---

## 2. 시스템 아키텍처 및 기술 스택

* **프론트엔드/백엔드:** Next.js (App Router), React, Tailwind CSS (또는 Vanilla CSS 기반 모던 디자인)
* **데이터베이스:** Supabase (PostgreSQL) - Row Level Security (RLS) 적용 등 보안 강화
* **호스팅 & 자동화:** Vercel (웹 호스팅) + GitHub Actions (Cron 스케줄러를 통한 자동화)
* **데이터 파이프라인 (수집/분석):** Python 3.11+ (데이터 수집 및 크롤링, Pandas 분석)

---

## 3. 단계별 개발 지침 (Execution Steps)

### PHASE 0: 프로젝트 환경 구축 및 인프라 세팅 (보완됨)
1. **Repository 및 디렉토리 구조 초기화:**
   - 프론트엔드(Next.js)와 백엔드/파이프라인(Python)을 명확히 분리된 폴더 구조로 설정 (예: `/web`, `/pipeline`)
2. **Supabase 프로젝트 세팅 및 데이터베이스 스키마 설계:**
   - 정규화된 스키마 설계: `complexes` (단지 정보), `transactions` (실거래가 내역), `listings` (현재 호가), `market_stats` (MDD 등 통계 요약) 
3. **환경변수(.env) 설정:** API Key, DB URL 등 보안 정보 철저히 분리.

### PHASE 1: 기초 데이터셋 구축 (`KB선도 50` 매핑)
1. **KB선도아파트 50 데이터 수집:**
   - 파이썬 스크립트를 작성하여 KB부동산 통계 자료 또는 최신 시가총액 상위 50개 단지 리스트(`단지명`, `지역/법정동`, `세대수`, `시가총액`)를 추출하여 CSV 저장 또는 DB로 바로 적재한다.
2. **공공데이터 & 네이버 부동산 ID 자동 매핑:**
   - 50개 단지의 **국토부 법정동코드(5자리) + 지번**과 네이버 부동산 단지 ID (`complexNo`)를 1:1로 매핑한 `master_complexes` 테이블을 구축한다.
   - 각 단지별 **역대 최고 실거래가 (전고점, 계약일)** 데이터를 국토부 과거 거래 이력 데이터를 파싱하여 자동으로 산출/저장한다.

### PHASE 2: 파이프라인 구축 (수집 & 급매 대조 Engine)
1. **국토부 실거래가 수집 Module (`collector_rtms.py`):**
   - `data.go.kr` API (`RTMSDataSvcAptTrade`)를 사용하여 매일 새벽 당월/전월의 실거래가 데이터를 수집한다.
   - 50개 대상 단지의 거래 건만 필터링하고, 단지별 [최신 실거래가, 직전 거래 대비 변동폭, 최고가 대비 하락률(MDD)]을 계산한다.
2. **네이버 부동산 최저 호가 크롤링 Module (`collector_naver.py`):**
   - 네이버 부동산 내부 API를 호출하여 50개 단지의 현재 등록된 **최저 매매 호가** (직거래/중복매물 제어)를 수집한다.
   - 요청 시 IP 차단을 방지하기 위해 랜덤 Delay(1~3초) 및 Header(User-Agent 변조 등) 설정을 준수한다.
3. **급매 판정 및 알림 로직 (`analyzer.py`):**
   - `현재 최저 매매 호가` < `최근 1~3개월 내 최저 실거래가` 조건을 충족할 경우 **'🔥 실거래 밑 급매'** 플래그를 지정하고 DB에 갱신한다.
4. **파이프라인 에러 모니터링 (보완됨):**
   - CronJob(데이터 수집) 중단 시 문제를 인지할 수 있도록 실패 시 디스코드/텔레그램 등 Webhook 알림(Logging) 파이프라인을 구축한다.

### PHASE 3: 프론트엔드 UI/UX 구현 (투명성 & 가독성 중심)
1. **메인 dashboard (랭킹 페이지):**
   - **하락률(MDD) 순위 정렬:** 최고가 대비 가장 많이 떨어진 순서대로 1~50위 단지 카드 노출.
   - **단지 카드 필수 요소:**
     - 단지명, 세대수, 전용면적(84㎡ 기준 등)
     - 역대 최고가 (날짜) vs 최신 실거래가 (날짜) vs 현재 최저 호가
   - **최고가 대비 하락률 (예: -24.5%)** 색상 대비 등을 통한 강력한 시각화 적용.
   - '실거래가 이하 급매' 존재 시 눈에 띄는 배지 부착 및 사용자 안내.
2. **데이터 왜곡 방지 및 투명성 강조 UI:**
   - *"왜 호가 지수와 실거래가는 다른가?"* 안내 인포그래픽 및 설명 툴팁 배치.
   - 취소 거래(허위 신고가) 및 특수관계인 직거래 여부를 구별할 수 있는 데이터 구분(Tag/Badge) 표시.
3. **SEO 및 페이지 퍼포먼스 최적화 (보완됨):**
   - 대장주 50개 아파트별 고유 URL 제공 (예: `/apt/[id]`) 및 동적 SEO 메타데이터(Title, Description 등)를 적용하여 검색엔진 유입 최적화.

---

## 4. 에이전트(Antigravity) 행동 수칙

* **모듈화:** 데이터 수집(`collector.py`), 데이터 변환/대조(`analyzer.py`), DB 업데이트(`db_manager.py`) 코드를 엄격히 분리하여 작성할 것.
* **에러 핸들링 & 폴백(Fallback):** API 응답 지연이나 네이버 크롤링 실패 시 웹 서비스 전체가 멈추지 않도록 예외 처리(Try-Except) 및 로깅 로직을 반드시 포함할 것. DB 업데이트 실패 시, 변경 이전의 데이터를 안전하게 유지하는 방어적 프로그래밍 준수.
* **비용 및 효율성:** 무료 타이어(Free Tier) 환경을 최대한 활용할 수 있도록 DB 쿼리를 최적화하고 정적 페이지 생성(ISR/SSG)을 적극 활용할 것.
* **프리미엄 UI/UX 철학 (보완됨):** 단순한 공공데이터 포털 형태를 피하고, 모던 웹 디자인 트렌드(세련된 컬러 팔레트, 부드러운 마이크로 애니메이션, 세밀한 타이포그래피 등)를 반영하여 신뢰감과 심미성을 동시에 충족시키는 강력한 웹 앱을 제작할 것.
