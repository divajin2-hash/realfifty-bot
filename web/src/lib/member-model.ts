export function safeReturn(value:string|null){if(!value||!value.startsWith('/')||value.startsWith('//')||/[\\\r\n]/.test(value))return '/account';try{const u=new URL(value,'https://realfifty.invalid');return u.origin==='https://realfifty.invalid'?u.pathname+u.search+u.hash:'/account';}catch{return '/account';}}
export type MemberItem={id?:string;complex_id?:string;area?:string;label?:string;report_date?:string;threshold?:number;baseline?:number;data_updated_at?:string;enabled?:boolean;content?:string;vote?:string;nickname?:string;created_at?:string;message?:string;read?:boolean};
