export const dynamic='force-dynamic';export async function GET(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return Response.json({email:false,kakao:false});try{const r=await fetch(url+'/auth/v1/settings',{headers:{apikey:key},signal:AbortSignal.timeout(5000),cache:'no-store'});if(!r.ok)throw new Error('unavailable');const d=await r.json();return Response.json({email:d.external?.email===true&&(process.env.VERCEL!=='1'||process.env.REALFIFTY_EMAIL_LOGIN_READY==='true'),kakao:d.external?.kakao===true},{headers:{'Cache-Control':'no-store'}});}catch{return Response.json({email:false,kakao:false,error:'인증 연결 상태를 확인하지 못했습니다.'},{status:503});}}
