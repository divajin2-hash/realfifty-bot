# KB50 Real Estate Pipeline Architecture & Master Plan (2026-07-27)

## 📌 1. 핵심 아키텍처 개요
본 문서는 KB50(RealFifty) 프롭테크 플랫폼의 심장인 '일일 데이터 자동화 파이프라인'의 최종 설계와 수집 방식을 영구적으로 기록하기 위해 작성되었습니다.

*   **목적:** 매일 새벽 최신 실거래가(국토부)와 최저호가(네이버)를 결합하여 정확한 단지/평형별 MDD(낙폭)를 계산.
*   **핵심 스크립트:** `32_master_daily_run.py` (전체 흐름을 제어하는 마스터 봇)

---

## 📌 2. 네이버 원자 단위 최저호가 수집 로직 (`10_full_pyeong_scraper.py`)
가장 많은 난항을 겪었던 네이버 부동산 데이터 수집은 **'원자 단위(Atomic) 매칭'**과 **'UI 클릭 배제 방식을 통한 초고속화'**로 최종 타결되었습니다.

### 2.1. 기존 방식의 한계 (URL spcMin/Max)
과거에는 `spcMin`, `spcMax` 파미러터를 URL에 넣어서 데이터를 모았기 때문에, 네이버가 마음대로 84A와 84B를 묶어서 섞어버리는 문제가 있었습니다.

### 2.2. 극복 방식 (평형 API 감청 & 정밀 필터링)
1. **단지별 네이버 ID 맵핑:** `naver_complex_mapping.json`을 통해 KB50 DB 단지 ID와 네이버 단지 번호(들)를 1:N으로 정밀하게 매핑합니다.
2. **Hidden API 감청:** 네이버 내부 통신 규격인 `/complexes/123/pyeongs` API를 직접 호출하여 해당 단지에 존재하는 모든 PTP(평형 타입 번호, ex: 84A, 84B)를 원자 단위로 추출합니다.
3. **클릭 없는 Direct Data Fetch:** 각 PTP 번호별로 `&ptpNo=1` 형태의 직접 필터를 걸고 로드한 뒤, 화면 내 렌더링 된 매물 카드(`div.price`) 텍스트만 초고속으로 긁어옵니다. 매물 상세페이지로 넘어가는 `card.click()`을 과감히 포기하여 **수집 속도를 3배 이상 폭발적으로 끌어올렸으며 안정성도 확보**했습니다.
4. **로컬 백업:** 중간 에러로 인한 DB 오염을 막기 위해 모든 수집 결과는 마지막에 `raw_daily_asks_2026-07-27.json` 형태로 한 번에 세이브(Dump) 합니다.

---

## 📌 3. 국토부 실거래가 수집 로직 (`31_daily_rtms_pro.py` & `33_historical_integrated_pro.py`)
네이버 호가와 1:1 매칭을 위한 국토부 과거 데이터베이스는 아래의 프롭테크 데이터 표준을 엄격하게 지키며 구성되었습니다.

### 3.1. 거래 유형 필터링 (직거래 배제)
API 내 `<dealingGbn>` 태그를 분석하여, 터무니없는 가격으로 형성되는 **'직거래', '지분거래'를 완벽히 배제**하고 누구나 인정 가능한 순수 중개거래만 통계에 편입합니다.

### 3.2. 소수점 면적 보존 매칭 (`exclusive_area_exact`)
국토부의 실거래 전용면적(`84.98㎡`)을 정수(`85㎡`)로 뭉뚱그리지 않고 소수점 끝자리까지 온전히 DB에 보존합니다. 이를 통해 네이버의 A타입(84.99), B타입(84.95) 등과 향후 한 치의 오차 없이 매칭할 수 있는 뼈대를 완성했습니다.

### 3.3. 재건축 추적 및 구형 데이터 필터링
*   아파트 단지 API뿐만 아니라, 철거 및 공사 중에 거래되는 **입주권/분양권 API**를 동시에 순회하여 추적합니다.
*   **Alias(별명) 필터링 기법:** '개포주공1단지' 같은 철거 전 과거 데이터가 역대 최고가(ATH)에 섞여 MDD 차트를 붕괴시키는 것을 막기 위해, *모든 Alias 매핑에서 재건축 전 구형 아파트 이름을 의도적으로 다 배제*했습니다.

---

## 📌 4. 통합 브릿지 / 결산 모듈 (`20_mdd_bridge.py`)
1. 생성된 네이버 호가 JSON 데이터와 국토부 DB의 역대 거래가를 양쪽에서 로드합니다.
2. 프론트엔드의 인지성을 위해 우선 Main Page용으로 **'동일 정수 그룹(예: 85㎡) 안에서 가장 싼 호가 vs 가장 비싼 실거래가'**를 산출하여 통합 MDD를 산출 및 DB(`pyeong_stats`) 갱신합니다.
3. 이를 통해 프론트엔드 코드의 대공사 없이 완벽한 백그라운드 데이터 세대교체를 이뤄냈습니다.

## 📌 5. 일일 자동화 적용 가이드
위 모든 통합 봇이 담긴 마스터 스크립트(`32_master_daily_run.py`)를 Github에 `Commit/Push` 함으로써 밤사이 Github Action 봇이 위 파이프라인(국토부 로드 -> 네이버 수집 -> MDD 결산 -> JSON 산출)을 자동 톱니바퀴처럼 굴리게 됩니다.
